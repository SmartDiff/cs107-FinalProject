language: python
python:
  # - "3.7"
  - "3.6"
  - "3.8"
before_install:
  - pip install pytest pytest-cov
  - pip install codecov
install:
  - pip install -r requirements.txt
script:
  - pytest --cov=./
after_success:
  - codecov
